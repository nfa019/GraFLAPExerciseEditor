<library>

<startouttext />
Please input now the components of the automaton  in formally correct notion into the text fields below. <p />

<endouttext />

<script type="loncapa/perl">

@auto=Automata::readautomaton($jff);
$type = 0;
if ($auto[0] eq "pda") { $type = 1; }
$result1=&cas('maxima',"set($auto[1]);"); # States
$result2=&cas('maxima',"set($auto[2]);"); # Alphabet
$result3=&cas('maxima',"set($auto[3]);"); # Stack Alphabet
$superset = &cas('maxima',"union(set($auto[2]),set($auto[3]));");
$result5=&cas('maxima',"set($auto[5]);"); # initials
$result6=&cas('maxima',"set($auto[6]);"); # finals
if ($auto[7] eq "nd") { $det = 1; } else { $det = 0;}
$tr = "Text";
$transitionsTeX = $auto[8];

$set4 = $auto[4];
$set4 =~ s/\(/[/g;
$set4 =~ s/\)/]/g;
$result4=&cas('maxima',"set($set4);"); # Delta
</script>
<startouttext />
the set of states  (states), das Eingabealphabet  (alphabet), 
<endouttext />

<script type="loncapa/perl">
if ($type) {
</script>

<startouttext />
the stack alphabet (Please use Z for the bottom symbol)  (stack alphabet), 
<endouttext />
<script type="loncapa/perl">
}
</script>

<startouttext />
>the inital state/s (initials), the set of final states (finals),<
<endouttext />

<script type="loncapa/perl">
if ($type<1) {
  if ( $det = 1) {

</script>
<startouttext />

and the transition function (delta).<p />
Write the elements as <b>(fromState, input, toState)</b>. Use E for the empty word, if applicable. <p />
e.g. (s0,a,s1),(s1,b,s2) and so on.<p />

<endouttext />
<script type="loncapa/perl">
}
if ( $det < 1) {
</script>

<startouttext />
and the transition relation
Write the elements as <b>(fromState, input, toState)</b>. Use E for the empty word, if applicable. <p />
e.g. (s0,a,s1),(s1,E,s2) and so on. <p />
<endouttext />
<script type="loncapa/perl">
  }
}

if ($type) {
</script>

  <startouttext />
and the transition relation (delta).<p />
Write the elements as <b>(fromState, input, pop, toState, push)</b>. Use Z for the bottm symbol and E for the empty word, if applicable. <p />
e.g. (s0,a,E,s1, aa),(s1, b, a,s2, E), (s1, b, Z,s2, b ) and so on. <p />
<endouttext />
<script type="loncapa/perl">
}
</script>


 <solved><startouttext />

$transitionsTeX

<endouttext /></solved>
</library>


